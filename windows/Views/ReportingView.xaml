<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="BudgetWarden_Windows.Views.ReportingView"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:local="using:BudgetWarden_Windows.Views"
    xmlns:viewModels="using:BudgetWarden_Windows.ViewModels">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <CommandBar
            HorizontalContentAlignment="Right"
            VerticalContentAlignment="Center">
            <CommandBar.Content>
                <controls:Segmented
                    x:Name="AmountSelector"
                    Margin="12,0,0,0"
                    SelectionMode="Single"
                    SelectionChanged="AmountSelector_SelectionChanged"
                    AutomationProperties.Name="Reporting amount"
                    AutomationProperties.AutomationId="ReportingAmountSelector">
                    <controls:SegmentedItem Content="Planned" />
                    <controls:SegmentedItem Content="Actual" />
                </controls:Segmented>
            </CommandBar.Content>
        </CommandBar>
        <ScrollViewer Grid.Row="1" Padding="12,15,12,0">
            <StackPanel Spacing="20" Padding="0,0,0,12">
                <ItemsRepeater ItemsSource="{Binding Data.Metrics, ElementName=Root}">
                    <ItemsRepeater.Layout>
                        <UniformGridLayout
                            MinItemWidth="170"
                            MinItemHeight="76"
                            MinColumnSpacing="16"
                            MinRowSpacing="16"
                            ItemsStretch="Fill" />
                    </ItemsRepeater.Layout>
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="viewModels:ReportingMetricViewModel">
                            <local:ReportingMetricCard Model="{x:Bind}" />
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
                <ItemsControl x:Name="ChartItems">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <local:ResponsiveGridPanel
                                MinItemWidth="320"
                                HorizontalSpacing="20"
                                VerticalSpacing="20" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Stretch" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="viewModels:ReportingChartViewModel">
                            <local:ReportingChartCard Model="{x:Bind}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
